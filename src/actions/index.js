const Reflux = require('reflux');

const ShebangEnchiladaActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'getResult'
]);

module.exports = ShebangEnchiladaActions;
